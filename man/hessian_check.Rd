% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hessian_check.R
\name{hessian_check}
\alias{hessian_check}
\title{Full Numerical Hessian and Correlation-Aware Standard Errors}
\usage{
hessian_check(
  optimal_params,
  params_range_min,
  params_range_max,
  dataset,
  centre,
  time_axis,
  dropout_matrix,
  e_matrix,
  h_dd,
  boundary_adjacent,
  se_diag,
  mu1_index,
  nu_index
)
}
\arguments{
\item{optimal_params}{Numerical vector of parameters at which the Hessian is evaluated.
Its length (i.e. number of parameters) is equal to \eqn{n_p}.}

\item{params_range_min}{Numerical vector of length equal to \eqn{n_p}, containing the minimum range of each parameter.}

\item{params_range_max}{Numerical vector of length equal to \eqn{n_p}, containing the maximum range of each parameter.}

\item{dataset}{Dataset containing the value of the regressors for all individuals in the study.}

\item{centre}{vector containing the group membership of each individual and that induces the clustering subdivision.}

\item{time_axis}{Temporal domain.}

\item{dropout_matrix}{Binary matrix of dimension (n_individuals, n_intervals).}

\item{e_matrix}{Matrix of dimension (n_individuals, n_intervals), see \code{params_se()}.}

\item{h_dd}{Discretization step for the numerical approximation of the second derivative of the log-likelihood function.}

\item{boundary_adjacent}{Integer vector of indices of boundary-adjacent parameters (as returned
by \code{gradient_check()$BoundaryAdjacent}), excluded from the interior covariance/SE computation.}

\item{se_diag}{Numerical vector of the existing diagonal-only standard errors (from \code{params_se()}),
used unchanged for boundary-adjacent parameters and for comparison for interior ones.}

\item{mu1_index, nu_index}{Position of \code{mu1} and \code{nu} in the parameter vector. Their raw 2x2
Hessian sub-block is reported separately (see Details), since Section 3.3's \code{mu1}/\code{nu} algebraic
coupling makes this pair of particular interest regardless of whether it ends up interior or
boundary-adjacent, where a full-matrix inversion may not be available.}
}
\value{
A list composed of:
\itemize{
\item Hessian: full \eqn{n_p \times n_p} numerical Hessian matrix (\code{NA} entries where a boundary
left no room for the required step).
\item InteriorIndices: integer vector of the non-boundary-adjacent parameter indices actually used
for the covariance/SE computation below.
\item SE_full: numerical vector of length \eqn{n_p}: correlation-aware standard errors for interior
parameters, \code{NA} for boundary-adjacent ones.
\item SE_ratio: \code{SE_full / se_diag} for interior parameters (\code{NA} for boundary-adjacent), summarizing
how much the full Hessian changes each interior parameter's standard error relative to the
diagonal-only approximation.
\item CorrelationMu1Nu: the (\code{mu1},\code{nu}) correlation implied by the raw 2x2 \code{mu1}/\code{nu} Hessian
sub-block (\code{NA} if that sub-block is not positive definite), reported independently of the
interior/boundary-adjacent split, since Section 3.3's \code{mu1}/\code{nu} coupling makes this pair of
particular interest regardless of which side of that split it falls on.
\item Mu1NuBlockPositiveDefinite: logical, whether the raw 2x2 \code{mu1}/\code{nu} Hessian sub-block is
positive definite (i.e. \code{CorrelationMu1Nu} is a valid correlation and not \code{NA}).
\item IsPositiveDefinite: logical, whether \code{-Hessian[InteriorIndices, InteriorIndices]} is positive
definite (i.e. the interior covariance matrix is a valid one).
}
}
\description{
Function for computing the full (not diagonal-only) finite-difference Hessian of the
log-likelihood function at a given parameter vector (typically the optimal parameters
returned by the multi-dimensional Powell/Brent optimization), together with the
resulting correlation-aware standard errors, for parameters that are not boundary-adjacent.
}
\details{
\code{params_se()} (\code{R/params_se_CI.R}) only approximates the diagonal of the Hessian, for
computational efficiency during repeated calls; this function computes the full matrix,
but only once, at a fixed parameter vector (typically the already-reported optimum), so the
additional cost (\eqn{O(n_p^2)} extra log-likelihood evaluations instead of \eqn{O(n_p)}) is
paid a single time.

Each parameter uses a symmetric, boundary-aware step \code{h <- min(h_dd, value - range_min, range_max - value)}, matching the step-size logic already adopted in \code{gradient_check()}
(\code{R/gradient_check.R}) to avoid the instability of an asymmetric clamped step.

Since a parameter sitting at/near a declared range boundary can make the full Hessian
ill-conditioned or non-negative-definite overall (a near-zero-curvature boundary direction
couples into every other row/column once the matrix is inverted, unlike the diagonal-only
approximation, which is immune to other parameters' degeneracy by construction), the
covariance matrix and its standard errors are computed only from the submatrix of
non-boundary-adjacent ("interior") parameters. Boundary-adjacent parameters (see
\code{gradient_check()}) keep their diagonal-only standard error (from \code{params_se()}) unchanged;
comparing the two is only meaningful for interior parameters.
}
\keyword{internal}
